#
# Makefile.cvs for nis_server module
#
configure: all
	./configure

all:
	y2tool y2automake
	aclocal
	autoconf
	automake -a

install: configure
	make
	make install

reconf: all
	./config.status --recheck
	./config.status
